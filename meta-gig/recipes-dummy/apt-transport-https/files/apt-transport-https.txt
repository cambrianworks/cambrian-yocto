apt-transport-https is a package dependency to something we wish to use without alteration. However, apt-transport-https no longer exists as a separate package since modern apt versions incorporate integrated https support. This package exists to satisfy the dependency without making real changes to the build image.
